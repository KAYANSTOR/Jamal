# إصلاح المزامنة عبر الأجهزة والسحابة + المزامنة التلقائية بعد عودة النت

## الملفات المعدّلة (ارفعها يدوياً)

| المسار في المستودع | الملف المحلي |
|--------------------|--------------|
| `src/lib/db.ts` | `src/lib/db.ts` |
| `src/App.tsx` | `src/App.tsx` |
| `src/context/AuthContext.tsx` | `src/context/AuthContext.tsx` |
| `src/components/SyncIndicator.tsx` | `src/components/SyncIndicator.tsx` |

## ماذا تم إصلاحه؟

### 1. عند العمل بدون نت ثم عودة الإنترنت
- الاستماع لحدث `online`
- بعد ~800ms يتم تشغيل **fullSync**:
  1. دفع كل عمليات الـ Outbox المعلقة إلى السحابة
  2. سحب البيانات الكاملة من السحابة (تظهر تغييرات الأجهزة الأخرى)

### 2. المزامنة بين الأجهزة
- بعد كل دفع ناجح → يتم سحب (`pullFromCloud`) تلقائياً
- مزامنة دورية كل **45 ثانية** طالما التطبيق مفتوح ومتصل
- `device_id` ثابت لكل جهاز

### 3. موثوقية الاتصال
- فحص حقيقي عبر `/api/health` (لا نعتمد فقط على `navigator.onLine`)
- استرجاع العمليات العالقة في حالة `SYNCING` بعد 5 دقائق
- إعادة محاولة بـ exponential backoff حتى 8 مرات

### 4. تجربة المستخدم
- مؤشر المزامنة يعرض:
  - Offline + عدد العمليات المعلقة
  - جاري المزامنة...
  - تعارض / فشل (قابل للضغط لإعادة المحاولة)
  - متصل + وقت آخر مزامنة
- زر يدوي للمزامنة الفورية

### 5. الجلسات الطويلة
- تجديد توكن Firebase كل 45 دقيقة حتى لا تفشل المزامنة بعد ساعات

## سلوك متوقع بعد الرفع

1. جهاز A يعمل Offline → يضيف صرف مواد → تظهر في الـ outbox.
2. يعود النت → خلال ثوانٍ يتم الدفع ثم السحب.
3. جهاز B (متصل) خلال 45 ثانية كحد أقصى يرى نفس البيانات (أو فور الضغط على مؤشر المزامنة).
4. لو انقطع النت مجدداً → التطبيق يستمر محلياً بدون مشكلة.

## ملاحظات

- الـ `/api/pull` الحالي هو full snapshot (مناسب للمرحلة الحالية). لاحقاً يمكن تحويله إلى delta بـ `since`.
- التعارضات (`CONFLICT`) مثل نقص المخزون تبقى ظاهرة في المؤشر حتى تُعالج يدوياً أو تُعاد المحاولة.
